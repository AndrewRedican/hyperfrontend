{
  "executors": {
    "install": {
      "implementation": "./src/executors/install/executor",
      "schema": "./src/executors/install/schema.json",
      "description": "Install dependencies in a self-contained application project."
    },
    "build": {
      "implementation": "./src/executors/build/executor",
      "schema": "./src/executors/build/schema.json",
      "description": "Build an application project using its local package.json scripts."
    },
    "serve": {
      "implementation": "./src/executors/serve/executor",
      "schema": "./src/executors/serve/schema.json",
      "description": "Serve an application project in development mode."
    },
    "pack-shell": {
      "implementation": "./src/executors/pack-shell/executor",
      "schema": "./src/executors/pack-shell/schema.json",
      "description": "Generate, compile, and pack a feature app's shell connector package."
    }
  }
}
