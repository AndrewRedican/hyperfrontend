/** How long an event dot takes to cross the wire unless the script says otherwise. */
export const EVENT_FLIGHT_MS = 700

/** How long a beat takes to cross the wire unless the script says otherwise. */
export const BEAT_FLIGHT_MS = 450

/** How long the feature takes to slide into the slot unless the script says otherwise. */
export const DOCK_MS = 1300

/** How long the feature takes to fade out of the slot unless the script says otherwise. */
export const UNDOCK_MS = 1000

/** How long the shutter takes to reach its next position unless the script says otherwise. */
export const GATE_MS = 500

/** How long the draft takes to cross to the host unless the script says otherwise. */
export const DRAFT_MS = 1000

/** How long the borders flash when the wire goes live. */
export const FLASH_MS = 700

/** How long an end of the wire glows after something arrives. */
export const GLOW_MS = 600

/** How long the dimension bracket takes to draw. */
export const BRACKET_MS = 500

/** How long after the bracket starts drawing the announcement leaves. */
export const PRESENT_DELAY_MS = 300

/** How long the feature's content takes to stretch to the announced size. */
export const FIT_MS = 500

/** How long a receipt or a saved document takes to pop in. */
export const POP_MS = 400

/** How long the close button stays pressed. */
export const PRESS_MS = 460

/** How long a tick's blink lasts. */
export const BLINK_MS = 300

/** How long a ring segment takes to fill. */
export const FILL_MS = 350

/** How long the feature takes to dim or brighten. */
export const DIM_MS = 300

/** How long before its first descent the gate is drawn in. */
export const GATE_REVEAL_MS = 300

/** Counted ticks that turn the ring to danger. */
export const MISS_BUDGET = 3
