{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "Lint Report executor schema",
  "description": "Run lint across all projects and generate an LLM-optimized report.",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "Output file path relative to workspace root.",
      "default": "lint-output.txt"
    },
    "affected": {
      "type": "boolean",
      "description": "Only lint affected projects instead of all.",
      "default": false
    },
    "maxFixes": {
      "type": "number",
      "description": "Number of files to suggest fixing first (quick wins).",
      "default": 5
    },
    "failOnError": {
      "type": "boolean",
      "description": "Return failure if lint errors are found (for CI/hooks).",
      "default": true
    }
  },
  "additionalProperties": false
}
