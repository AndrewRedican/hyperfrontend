/**
 * Schema types for the typecheck executor.
 */
export interface TypecheckExecutorOptions {
  /**
   * Path to the TypeScript configuration file.
   * Defaults to {projectRoot}/tsconfig.lib.json.
   */
  tsConfig?: string
}
