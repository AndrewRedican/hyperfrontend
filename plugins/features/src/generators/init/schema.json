{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to convert into a hyperfrontend feature"
    },
    "configPath": {
      "type": "string",
      "description": "Path where the feature configuration and contracts should be stored",
      "default": "hyperfrontend"
    },
    "skipInstall": {
      "type": "boolean",
      "description": "Skip installing dependencies",
      "default": false
    }
  },
  "required": ["project"]
}
