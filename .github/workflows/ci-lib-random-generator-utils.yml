# Build status reporter for lib-random-generator-utils
# This workflow does NOT build - it reports status from ci-libraries.yml matrix build
name: lib-random-generator-utils

on:
  workflow_run:
    workflows: [libraries]
    types: [completed]
    branches: [main]

permissions:
  actions: read

jobs:
  status:
    uses: ./.github/workflows/_lib-status.yml
    with:
      project-name: lib-random-generator-utils
      library-path: libs/utils/random-generator
