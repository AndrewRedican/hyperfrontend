# Build status reporter for lib-list-utils
# This workflow does NOT build - it reports status from ci-libraries.yml matrix build
name: lib-list-utils

on:
  workflow_run:
    workflows: [libraries]
    types: [completed]
    branches: [main]

permissions:
  actions: read

jobs:
  status:
    uses: ./.github/workflows/_lib-status.yml
    with:
      project-name: lib-list-utils
      library-path: libs/utils/list
