name: lib-list-utils

on:
  push:
    branches: [main]
    paths:
      - 'libs/utils/list/**'
      - '.github/workflows/ci-lib-list-utils.yml'
      - '.github/workflows/_lib-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-lib-list-utils-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_lib-ci.yml
    with:
      project-name: lib-list-utils
      coverage-path: ./coverage/libs/utils/list/lcov.info
      coverage-flag: list-utils
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
