name: lib-data-utils

on:
  push:
    branches: [main]
    paths:
      - 'libs/utils/data/**'
      - '.github/workflows/ci-lib-data-utils.yml'
      - '.github/workflows/_lib-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-lib-data-utils-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_lib-ci.yml
    with:
      project-name: lib-data-utils
      coverage-path: ./coverage/libs/utils/data/lcov.info
      coverage-flag: data-utils
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
