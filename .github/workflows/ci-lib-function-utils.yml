name: lib-function-utils

on:
  push:
    branches: [main]
    paths:
      - 'libs/utils/function/**'
      - '.github/workflows/ci-lib-function-utils.yml'
      - '.github/workflows/_lib-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-lib-function-utils-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_lib-ci.yml
    with:
      project-name: lib-function-utils
      coverage-path: ./coverage/libs/utils/function/lcov.info
      coverage-flag: function-utils
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
