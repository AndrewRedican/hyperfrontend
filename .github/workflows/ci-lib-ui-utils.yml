name: lib-ui-utils

on:
  push:
    branches: [main]
    paths:
      - 'libs/utils/ui/**'
      - '.github/workflows/ci-lib-ui-utils.yml'
      - '.github/workflows/_lib-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-lib-ui-utils-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/_lib-ci.yml
    with:
      project-name: lib-ui-utils
      coverage-path: ./coverage/libs/utils/ui/lcov.info
      coverage-flag: ui-utils
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
