import './styles/fish.css'
// why: Registering the element is the whole bootstrap — `index.html` already carries the tag, and the browser upgrades it the moment this module runs.
import './koi/koi-fish'
