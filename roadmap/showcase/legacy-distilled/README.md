# Legacy Distilled

> **Placeholder — populated by [07 — Legacy Distillation](../07-legacy-distillation.md).**

The home for value salvaged from the untracked `_/` directory before it is deleted. Two kinds of content will land here:

1. **Distilled-idea stubs** — one `.md` per worthwhile pattern, tagged `adopt` / `adapt` / `reimagine`, with a pointer to the plan it feeds.
2. **Tier-1 keepers (verbatim)** — committed copies of the genuinely reusable complete artifacts (the fluent Connector API source, contract/action patterns, the scenario-mock model, the `LEGACY_PROJECTS_ANALYSIS.md` synthesis).

Because `_/` is gitignored, committing these here is what makes deleting the 162 MB safe.

## Index

| Item       | Kind | Disposition | Feeds |
| ---------- | ---- | ----------- | ----- |
| _none yet_ |      |             |       |
