# Findings Registry

Friction found while building demos against published `@hyperfrontend/*` packages, filed via the `demo-findings` skill.

## Open

| ID    | Title                                                                       | Category     | Severity | Surfaced by                 | Status | Disposition |
| ----- | --------------------------------------------------------------------------- | ------------ | -------- | --------------------------- | ------ | ----------- |
| F-001 | `hf build` fails in a fresh project — rollup native binding not installed   | packaging    | blocker  | demo-clock 0.3.0 re-consume | open   | —           |
| F-002 | `hf build` fails in a fresh project — TypeScript plugin cannot find `tslib` | packaging    | blocker  | demo-clock 0.3.0 re-consume | open   | —           |
| F-003 | Generated shell handle type omits `request`/`handle`/`isDirty`              | api-friction | medium   | demo-clock 0.3.0 re-consume | open   | —           |
| F-004 | `defineConfig` rejects the config keys the CLI actually reads               | api-friction | medium   | demo-clock 0.3.0 re-consume | open   | —           |

## Resolved

| ID  | Title | Resolution |
| --- | ----- | ---------- |

Declined findings move to [declined.md](declined.md) with a one-line reason.
