/**
 * Bin subdomain facade: JS bin synthesis (Phase 6) composed via {@link runBinPhase}.
 * Native (Node SEA) emission is added in a later phase and re-exported from
 * `@hyperfrontend/builder/bin/native`.
 *
 * @module @hyperfrontend/builder/bin
 */
export { runBinPhase } from './run-bin-phase'
