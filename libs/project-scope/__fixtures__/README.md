# Test Fixtures

This directory contains test fixture projects used by unit and integration tests.
Each subdirectory represents a different project type or configuration.

> **TODO:** DELETE THIS FILE WHEN FIXTURES ARE ADDED

## Planned fixtures (from 09-testing-strategy.md)

- `minimal-project/` - Minimal valid project with package.json
- `react-vite/` - React + Vite project
- `angular-nx/` - Angular + NX project
- `monorepo/` - Multi-project workspace
- `legacy-webpack/` - Legacy webpack configuration
- `empty/` - Empty directory for edge case testing
