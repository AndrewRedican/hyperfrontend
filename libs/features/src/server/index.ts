/**
 * Dev server and debug UI for testing host/hostee interactions.
 *
 * The server surface is implemented in a later phase (see roadmap plan 06 — Dev Server);
 * this entry point reserves the `@hyperfrontend/features/server` subpath.
 *
 * @module @hyperfrontend/features/server
 */
export {}
