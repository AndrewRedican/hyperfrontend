# Server

Dev server and debug UI for testing host/hostee interactions — static file serving, display-mode controls, and message-traffic inspection.

> Status: entry point reserved. The server surface is implemented in a later phase (see roadmap plan 06 — Dev Server).

```ts
import {} from '@hyperfrontend/features/server'
```
