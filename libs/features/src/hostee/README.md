# Hostee

Hostee-side SDK for feature apps — feature initialization, contract declaration, and feature lifecycle.

> Status: entry point reserved. The hostee surface is implemented in a later phase (see roadmap plan 03 — Core SDK).

```ts
import {} from '@hyperfrontend/features/hostee'
```
