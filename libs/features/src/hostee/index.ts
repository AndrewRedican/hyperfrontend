/**
 * Hostee-side SDK for feature apps (feature initialization and lifecycle).
 *
 * The hostee surface is implemented in a later phase (see roadmap plan 03 — Core SDK);
 * this entry point reserves the `@hyperfrontend/features/hostee` subpath.
 *
 * @module @hyperfrontend/features/hostee
 */
export {}
