# Host

Host-side SDK for embedding hyperfrontend features — shell factory, display modes (embedded, dialog, popup, standalone), iframe utilities, and open/close lifecycle.

> Status: entry point reserved. The host surface is implemented in a later phase (see roadmap plan 03 — Core SDK).

```ts
import {} from '@hyperfrontend/features/host'
```
