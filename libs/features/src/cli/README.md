# CLI

Programmatic entry point for the hyperfrontend features CLI — `init`, `build`, and `dev` commands plus `feature.config.*` handling.

> Status: entry point reserved. The CLI surface is implemented in a later phase (see roadmap plan 05 — CLI); the executable `bin` is gated on the CLI/bin execution research (roadmap plan 02).

```ts
import {} from '@hyperfrontend/features/cli'
```
