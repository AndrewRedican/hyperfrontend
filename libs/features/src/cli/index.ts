/**
 * Programmatic entry point for the hyperfrontend features CLI (init, build, dev).
 *
 * The CLI surface is implemented in a later phase (see roadmap plan 05 — CLI), and the
 * executable `bin` is gated on the CLI/bin execution research (roadmap plan 02); this
 * entry point reserves the `@hyperfrontend/features/cli` subpath.
 *
 * @module @hyperfrontend/features/cli
 */
export {}
