/**
 * Programmatic entry point for the hyperfrontend features CLI (init, build, dev).
 *
 * The CLI surface is not yet implemented; this entry point reserves the
 * `@hyperfrontend/features/cli` subpath.
 *
 * @module @hyperfrontend/features/cli
 */
export {}
